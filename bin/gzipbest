#!/usr/bin/env sh
# This script is part of Martin V\"ath's archwrap project.
set -u
which=`command -v which` || which=
[ x"$which" = x'which' ] && gzip=`which gzip` \
		|| gzip=`command -v gzip` || gzip=
exec "${gzip:-gzip}" --best ${1+"$@"}
